<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newburger="{{devoured}}">
		{{#if devoured}}Yum Yum!{{else}}Devour!{{/if}}
	</button>
	
<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
	
</li>
